import { Landing } from "@/components/marketing/landing";

// Public marketing entry. Authenticated product lives under the (dashboard) group.
export default function HomePage() {
  return <Landing />;
}
